# Top-level dev / test dependencies. See requirements.txt for the
# pinning policy.
httpx>=0.27,<2
pytest>=9.0,<11
pytest-django>=4.9,<6
ruff>=0.6,<2
pip-audit>=2.7,<4
# pip-tools provides ``pip-compile``, which materialises the
# ``requirements.lock`` / ``requirements-dev.lock`` files with
# ``--generate-hashes``. Operators refresh the lock with:
#
#   pip-compile --generate-hashes --output-file=requirements.lock requirements.txt
#   pip-compile --generate-hashes --allow-unsafe --output-file=requirements-dev.lock requirements-dev.txt
#
# The lock files are the source of truth for CI and the deploy
# (ASVS V14.2.3). Bumping a top-level range here without
# regenerating the lock will fail CI on the hash mismatch step.
pip-tools>=7,<9
# bandit runs the same security pattern catalog ruff exposes via the
# "S" lints, but with a different rule engine. CI runs both so a gap
# in one catches the other; if bandit catches something ruff misses
# the operator gets a fix path either way.
bandit>=1.7,<3
# Phase 1 #1 of the 2026-06-20 roadmap — pre-commit hooks (ruff,
# ruff-format, detect-secrets, hygiene). Install once per checkout
# with ``pre-commit install``; the hook then runs on every commit.
pre-commit>=4.0,<6
# Pre-commit hooks for catching accidentally committed credentials.
# Baseline at ``.secrets.baseline`` lists allowed fixtures.
detect-secrets>=1.5,<2
# Phase 1 #2 — code coverage measurement in CI with a floor.
# Local invocation: ``coverage run --source=src -m pytest && coverage report``.
coverage>=7.5,<9
# Phase 3 #6 — static type checking. ``django-stubs`` plugs into
# mypy for ORM-level type narrowing. Config + per-module overrides
# live in pyproject.toml [tool.mypy].
mypy>=2.0,<3
django-stubs>=6.0,<7
# Transitive constraint: msgpack <1.2.1 has GHSA-6v7p-g79w-8964
# (memory corruption in C ext). pip-audit on the dev lockfile
# flags it; pin floor to 1.2.1 so the next pip-compile picks the
# patched version. detect-secrets and cyclonedx pull msgpack as
# a dep.
msgpack>=1.2.1,<2
