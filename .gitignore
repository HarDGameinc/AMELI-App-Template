.venv/
.venv-*/
.test-deps/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.mypy_cache/
# Phase 1 #2 — coverage local artefacts (the .coverage SQLite
# db and the optional HTML report). CI generates its own each
# run; nothing useful to commit.
.coverage
.coverage.*
htmlcov/
.env
data/
logs/
backups/
*.egg-info/
# Local dev / e2e SQLite databases (AMELI_APP_SQLITE_PATH fallback,
# pytest-django test DBs). Never committed — Postgres is the real DB.
*.sqlite3
